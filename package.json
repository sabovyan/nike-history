{
	"name": "nike-history",
	"version": "1.0.0",
	"description": "A short history of nike",
	"main": "index.js",
	"scripts": {
		"start": "parcel src/index.html",
		"build": "parcel build src/index.html --out-dir docs --public-url ./",
		"predeploy": "rm -rf dist && parcel build src/index.html --public--url /parcel-gh-pages-deploy",
		"deploy": "gh-pages -d dist",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "sabovyan",
	"license": "ISC",
	"devDependencies": {
		"cssnano": "^4.1.10",
		"gh-pages": "^3.1.0",
		"parcel-bundler": "^1.12.4"
	}
}
